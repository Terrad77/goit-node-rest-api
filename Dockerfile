# Вказуємо базовий образ Node.js
FROM node:18

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо package.json та package-lock.json у робочу директорію
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо решту коду додатку у робочу директорію
COPY . .

# Відкриваємо порт, на якому буде працювати додаток
EXPOSE 3000

# Вказуємо команду для запуску додатку
CMD ["npm", "start"]